import { useEffect, useState } from "react";
export default function Login() { const [register,setRegister]=useState(false); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [name,setName]=useState(""); const [message,setMessage]=useState(""); useEffect(()=>{if(window.location.hash.includes("access_token")||new URLSearchParams(window.location.search).has("code")){setMessage("Email confirmed — you are now registered. You can sign in.");setRegister(false)}},[]); const submit=async(e:any)=>{e.preventDefault();setMessage("Working...");const r=await fetch(register?"/api/auth/register":"/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password,name})});const v=await r.json();if(!r.ok){setMessage(v.error||"Unable to continue");return}if(v.session?.access_token){localStorage.setItem("pes_access_token",v.session.access_token);window.location.replace("/")}else setMessage("Account created. Check your email to confirm it, then sign in.")}; return <main className="flex min-h-screen items-center justify-center bg-slate-50 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border bg-white p-8 shadow-sm"><div className="text-xl font-bold text-indigo-700">PES</div><h1 className="mt-8 text-2xl font-bold">{register?"Create account":"Welcome back"}</h1><p className="mt-2 text-sm text-slate-500">{register?"Create an account to manage evaluations.":"Sign in to manage performance evaluations."}</p>{register&&<label className="mt-8 block text-sm font-medium">Name<input required className="mt-2 w-full rounded-lg border p-3" value={name} onChange={e=>setName(e.target.value)} /></label>}<label className="mt-6 block text-sm font-medium">Email<input required className="mt-2 w-full rounded-lg border p-3" type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="mt-4 block text-sm font-medium">Password<input required minLength={6} className="mt-2 w-full rounded-lg border p-3" type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><button className="mt-6 w-full rounded-lg bg-indigo-600 px-4 py-3 font-semibold text-white">{register?"Create account":"Sign in"}</button>{message&&<p className="mt-4 text-sm text-slate-600">{message}</p>}<button type="button" onClick={()=>{setRegister(!register);setMessage("")}} className="mt-4 w-full text-sm text-indigo-700">{register?"Already have an account? Sign in":"Need an account? Create one"}</button></form></main>; }
