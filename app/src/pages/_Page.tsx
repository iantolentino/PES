import { Link, NavLink } from "react-router";

const links = [["/", "Dashboard"], ["/employees", "Employees"], ["/evaluations", "Evaluations"], ["/clients", "Clients"], ["/departments", "Departments"], ["/audit", "Audit logs"]];

export default function Page({ title, children }: { title: string; children?: React.ReactNode }) {
  return <div className="min-h-screen bg-slate-50 text-slate-900"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r bg-white p-6 md:block"><Link to="/" className="text-xl font-bold text-indigo-700">PES</Link><p className="mt-1 text-xs text-slate-500">Performance Evaluation System</p><nav className="mt-10 space-y-1">{links.map(([href, label]) => <NavLink key={href} to={href} className={({ isActive }) => `block rounded-lg px-3 py-2 text-sm ${isActive ? "bg-indigo-50 font-semibold text-indigo-700" : "text-slate-600 hover:bg-slate-50"}`}>{label}</NavLink>)}</nav><div className="absolute bottom-6 left-6 right-6 rounded-lg bg-slate-50 p-3 text-xs text-slate-500">Signed in as<br /><strong className="text-slate-700">Demo Admin</strong></div></aside><main className="md:ml-64"><header className="border-b bg-white px-6 py-5 md:px-10"><p className="text-sm text-slate-500">Performance Evaluation System</p><h1 className="text-2xl font-bold">{title}</h1></header><div className="p-6 md:p-10">{children ?? <p className="text-slate-500">No records yet.</p>}</div></main></div>;
}
